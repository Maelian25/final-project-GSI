import React from "react";

const Settings: React.FC = () => {
    return (
        <div style={{marginTop: '5rem'}}>
            <h1>Settings</h1>
            
        </div>
    );
};

export default Settings;